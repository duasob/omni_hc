#!/bin/bash
#PBS -N darcy_dirichlet_ansatz_zero_Transolver_distance_power
#PBS -l select=1:ncpus=4:mem=24gb:ngpus=1
#PBS -l walltime=10:00:00
#PBS -j oe

set -euo pipefail

PROJECT_DIR="${PROJECT_DIR:-${PBS_O_WORKDIR:-$PWD}}"
cd "$PROJECT_DIR"

DEVICE="${DEVICE:-auto}" bash scripts/hpc/runner.sh #runner.sh
