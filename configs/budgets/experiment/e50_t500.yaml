# Train target 500: ntrain = 500 train + 100 val (val is carved out of ntrain).
data:
  ntrain: 600
  ntest: 200

training:
  batch_size: 4
  val_size: 100
  num_epochs: 50
  seed: 42

evaluation:
  batch_size: 4

wandb_logging:
  wandb: true
  log_every: 50
  image_log_every: 1
