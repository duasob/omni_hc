# Train target 100: ntrain = 100 train + 20 val (val is carved out of ntrain).
data:
  ntrain: 120
  ntest: 200

training:
  batch_size: 4
  val_size: 20
  num_epochs: 10
  seed: 42

evaluation:
  batch_size: 4

wandb_logging:
  wandb: true
  log_every: 10
  image_log_every: 1
