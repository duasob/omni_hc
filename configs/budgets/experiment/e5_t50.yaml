# Train target 50: ntrain = 50 train + 10 val (val is carved out of ntrain).
data:
  ntrain: 60
  ntest: 200

training:
  batch_size: 4
  val_size: 10
  num_epochs: 5
  seed: 42

evaluation:
  batch_size: 4

wandb_logging:
  wandb: true
  log_every: 5
  image_log_every: 1
