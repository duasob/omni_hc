# Train target 900: ntrain = 900 train + 100 val (val is carved out of ntrain).
data:
  ntrain: 1000
  ntest: 200

training:
  batch_size: 4
  val_size: 100
  num_epochs: 100
  seed: 42

evaluation:
  batch_size: 4

wandb_logging:
  wandb: true
  log_every: 100
  image_log_every: 1
