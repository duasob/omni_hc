data:
  ntrain: 1000
  ntest: 200

training:
  batch_size: 4
  num_epochs: 10
  val_size: 100
  seed: 42

evaluation:
  batch_size: 4

wandb_logging:
  wandb: true
  log_every: 10
  image_log_every: 5
