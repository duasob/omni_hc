extends:
  - fno_small.yaml
  - ../../constraints/pipe_stream_function_boundary.yaml

paths:
  output_dir: "outputs/pipe/fno_small_stream_boundary"

wandb_logging:
  run_name: "pipe_fno_small_stream_boundary"
