name: "pipe_fno_stream_boundary"
benchmark: "pipe"
backbone: "FNO"
constraint: "pipe_stream_function_boundary"
budget: "debug"

overrides:
  paths:
    output_dir: "outputs/pipe/fno_stream_boundary"
  wandb_logging:
    run_name: "pipe_fno_stream_boundary"

