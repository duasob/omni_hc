extends:
  - ../../benchmarks/pipe/base.yaml
  - ../../backbones/FNO.yaml

model:
  args:
    unified_pos: 0
    task: "steady"
    loader: "pipe"
    geotype: "structured_2D"
    fun_dim: 2
    out_dim: 1
    n_hidden: 32
    modes: 12

paths:
  output_dir: "outputs/pipe/fno_small"

wandb_logging:
  wandb: false
