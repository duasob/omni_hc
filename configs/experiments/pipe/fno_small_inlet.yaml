name: "pipe_fno_inlet"
benchmark: "pipe"
backbone: "FNO"
constraint: "pipe_inlet_parabolic"
budget: "debug"

overrides:
  paths:
    output_dir: "outputs/pipe/fno_inlet"
  wandb_logging:
    run_name: "pipe_fno_inlet"

