name: "pipe_fno_wall"
benchmark: "pipe"
backbone: "FNO"
constraint: "pipe_wall_no_slip"
budget: "debug"

overrides:
  paths:
    output_dir: "outputs/pipe/fno_wall"
  wandb_logging:
    run_name: "pipe_fno_wall"

