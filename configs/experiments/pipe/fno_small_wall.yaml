name: "pipe_fno_wall"
benchmark: "pipe"
backbone: "FNO"
constraint: "structured_wall_dirichlet_ansatz"
budget: "debug"

overrides:
  paths:
    output_dir: "outputs/pipe/fno_wall"
  wandb_logging:
    run_name: "pipe_fno_wall"

