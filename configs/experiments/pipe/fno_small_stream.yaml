name: "pipe_fno_stream"
benchmark: "pipe"
backbone: "FNO"
constraint: "pipe_stream_function"
budget: "debug"

overrides:
  paths:
    output_dir: "outputs/pipe/fno_stream"
  wandb_logging:
    run_name: "pipe_fno_stream"

