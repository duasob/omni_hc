name: "pipe_fno_ux_boundary"
benchmark: "pipe"
backbone: "FNO"
constraint: "pipe_ux_boundary_ansatz"
budget: "debug"

overrides:
  paths:
    output_dir: "outputs/pipe/fno_ux_boundary"
  wandb_logging:
    run_name: "pipe_fno_ux_boundary"

