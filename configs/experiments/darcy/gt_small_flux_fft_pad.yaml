name: "darcy_galerkin_transformer_flux_fft_pad"
benchmark: "darcy"
backbone: "Galerkin_Transformer"
constraint: "darcy_flux_constraint"
budget: "debug"
optuna: "darcy_flux_fft_pad"

overrides:
  constraint:
    # This experiment is the spectral + reflect-pad baseline; pin it explicitly
    # since the shared constraint config now defaults to the FD path.
    stream_derivative: "spectral"
    padding: 8
  paths:
    output_dir: "outputs/darcy/galerkin_transformer_flux_fft_pad"
  wandb_logging:
    run_name: "darcy_galerkin_transformer_flux_fft_pad"

