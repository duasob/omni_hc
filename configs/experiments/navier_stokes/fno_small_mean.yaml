name: "navier_stokes_fno_mean"
benchmark: "navier_stokes"
backbone: "FNO"
constraint: "mean_correction"
budget: "debug"
optuna: "mean_correction"

overrides:
  paths:
    output_dir: "outputs/navier_stokes/fno_mean"
  constraint:
    mode: "post_output"
  wandb_logging:
    run_name: "navier_stokes_fno_mean"
    image_log_every: 10

