extends:
  - fno_small.yaml
  - ../../constraints/elasticity_deviatoric_stress.yaml

paths:
  output_dir: "outputs/elasticity/fno_small_deviatoric_stress"

wandb_logging:
  run_name: "elasticity_fno_small_deviatoric_stress"
